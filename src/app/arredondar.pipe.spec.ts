import { ArredondarPipe } from './arredondar.pipe';

describe('ArredondarPipe', () => {
  it('create an instance', () => {
    const pipe = new ArredondarPipe();
    expect(pipe).toBeTruthy();
  });
});
